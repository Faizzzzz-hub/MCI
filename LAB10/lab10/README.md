# lab10

This directory is the lowercase `lab10` folder requested. It was created inside the workspace at `LAB10/lab10` so it can be populated and committed.

Next steps (run from the repository root `/home/administrator/st/MCI`):

1. Stage and commit this README so the folder is tracked by git.
2. Push to your remote (e.g., `origin`) and branch (e.g., `master` or `main`).

If you'd like, you can replace this README with your lab files or add additional files here.
